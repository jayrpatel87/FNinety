# FalseNinety

Monorepo scaffold for FalseNinety project.

## Services

- backend: FastAPI service in `backend/app`.

## Quick start

Build and run with Docker Compose:

```bash
docker-compose up --build
```

Then open http://localhost:8000
